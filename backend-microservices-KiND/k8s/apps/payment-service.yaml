apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: letsplaypro
spec:
  replicas: 2
  selector:
    matchLabels: { app: payment-service }
  template:
    metadata:
      labels: { app: payment-service }
      annotations:
        # k8s.grafana.com/* (not the older prometheus.io/* convention) — this is the actual
        # default annotation the Grafana Kubernetes Monitoring Helm chart's Annotation
        # Autodiscovery feature looks for. No ServiceMonitor CRD, no Prometheus Operator
        # required. See k8s/observability/README.md.
        k8s.grafana.com/scrape: "true"
        k8s.grafana.com/metrics.portNumber: "3005"
        k8s.grafana.com/metrics.path: "/metrics"
        # Explicit job name so Prometheus's "job" label is exactly "payment-service" — predictable and
        # stable to query on, rather than whatever the chart would otherwise derive.
        k8s.grafana.com/job: "payment-service"
    spec:
      containers:
        - name: payment-service
          image: letsplaypro/payment-service:local
          imagePullPolicy: IfNotPresent
          ports: [{ containerPort: 3005 }]
          envFrom:
            - configMapRef: { name: letsplaypro-config }
          env:
            - name: PORT
              value: "3005"
            - name: MONGO_URI
              valueFrom: { configMapKeyRef: { name: letsplaypro-config, key: MONGO_URI_PAYMENT } }
            - name: STRIPE_SECRET_KEY
              valueFrom: { secretKeyRef: { name: letsplaypro-secrets, key: STRIPE_SECRET_KEY } }
            - name: STRIPE_WEBHOOK_SECRET
              valueFrom: { secretKeyRef: { name: letsplaypro-secrets, key: STRIPE_WEBHOOK_SECRET } }
          readinessProbe:
            httpGet: { path: /health, port: 3005 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 3005 }
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits: { cpu: "500m", memory: "256Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: letsplaypro
spec:
  selector: { app: payment-service }
  ports: [{ port: 3005, targetPort: 3005 }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: payment-service
  namespace: letsplaypro
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: payment-service
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
