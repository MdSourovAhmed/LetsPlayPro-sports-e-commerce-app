PORT=3000
JWT_SECRET=7111777

AUTH_SERVICE_URL=http://localhost:3001
PRODUCT_SERVICE_URL=http://localhost:3002
ORDER_SERVICE_URL=http://localhost:3003
DASHBOARD_SERVICE_URL=http://localhost:3004
PAYMENT_SERVICE_URL=http://localhost:3005

# Backs every rate limiter (see middleware/rateLimitStore.js) — required for rate limits to
# apply correctly once you run more than one gateway replica.
REDIS_URL=redis://localhost:6379

# Comma-separated frontend origins allowed to send credentialed requests (needed for the
# refresh-token cookie — see index.js's CORS setup).
CORS_ORIGINS=http://localhost:5173,http://localhost:5174
