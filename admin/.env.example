# Base URL for the backend API. Point this at your gateway in production.
VITE_API_BASE_URL=http://localhost:3000/api

# How long before showing a "taking longer than usual" hint on slow requests (ms)
VITE_SLOW_REQUEST_THRESHOLD=4000
